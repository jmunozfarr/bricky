# Synthetic LDraw fixtures

`demo-steps.ldr` is a small, hand-authored MPD fixture used only to exercise Bricky's browser-side LDraw loading and building-step controls. Its embedded submodels contain direct geometry, so it does not use or represent an official LEGO model and does not require the official LDraw parts library.
